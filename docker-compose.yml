services:
  bruno-bot:
    build: .
    container_name: bruno-bot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
